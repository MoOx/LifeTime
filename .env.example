# Copy to `.env` at the project root (fastlane loads it automatically).
# NEVER COMMIT `.env`, add it to your .gitignore.

# --- app identity ----------------------------------------------------------
# Capitalised on purpose: the App Store profile in MoOx/certificates is
# `AppStore_io.moox.LifeTime`, and Apple bundle IDs are case-sensitive.
APP_IDENTIFIER=io.moox.LifeTime
# Android kept lowercase, matching the package already published on Play.
ANDROID_PACKAGE_NAME=io.moox.lifetime
APPLE_TEAM_ID=DHLLCP4Q6E
APPLE_ID=

# --- certificates repository -----------------------------------------------
CERTS_REPO_URL=git@github.com:MoOx/certificates.git

# --- secrets (the only two real secrets on the project side) ---------------
# fastlane match passphrase (iOS certificates and profiles)
MATCH_PASSWORD=
# passphrase for the secrets/ directory of the certificates repository
SECRETS_PASSPHRASE=

# --- convenience -----------------------------------------------------------
FASTLANE_XCODEBUILD_SETTINGS_TIMEOUT=120
FASTLANE_SKIP_UPDATE_CHECK=1
