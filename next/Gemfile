source "https://rubygems.org"

gem "fastlane"
gem "cocoapods"

# xcodeproj comes in through fastlane anyway, but it is pinned explicitly here:
# it is the gem that rewrites the signing settings after `expo prebuild`.
gem "xcodeproj"
